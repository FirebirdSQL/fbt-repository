{
'id': 'bugs.core_3502',
'qmid': None,
'tracker_id': 'CORE-3502',
'title': 'DROP VIEW ignores the existing non-column dependencies',
'description': '',
'min_versions': '2.5.1',
'versions': [
{
 'firebird_version': '2.5.1',
 'platform': 'All',
 'page_size': '4096',
 'init_script': """create view v (id) as
select rdb$relation_id from rdb$database;
commit;
set term !!;
create procedure p as
  declare id int;
begin
  for select 1 from v into :id do begin end
end!!
set term ;!!
commit;
""",
 'test_type': 'ISQL',
 'test_script': """execute procedure p;
commit;
drop view v;
commit;
""",
 'expected_stderr': """Statement failed, SQLSTATE = 42000

unsuccessful metadata update

-cannot delete

-TABLE V

-there are 1 dependencies

"""
}
]
}
